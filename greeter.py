# storing the prompt and passing that as input

prompt = "if you tell us whoyou are, we can personalise the msg u see"
prompt += "\nwhat is you first name? "

name = input(prompt)
print("\n hello " + name + "!")
